/**
 * `@harf/native` — React Native adapter for Harf.
 *
 * The no-restart direction switch, logical `StyleSheet` integration, the icon
 * mirroring registry and the gesture/animation helpers land in M2. This entry
 * currently re-exports the direction primitives from `@harf/core`.
 *
 * @packageDocumentation
 */

export { DIRECTIONS, isDirection, oppositeDirection, type Direction } from '@harf/core';
